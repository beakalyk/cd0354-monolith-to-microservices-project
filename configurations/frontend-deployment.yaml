apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  selector:
    matchLabels:
      run: frontend
  replicas: 2
  template:
    metadata:
      labels:
        run: frontend
    spec:
      containers:
      - name: frontend
        image: docker.io/beakalyk/udagram-frontend:local
        imagePullPolicy : always
        ports:
        - containerPort: 80
        resources:
          requests:
           memory: "128Mi"
           cpu: "250m"
          limits:
            memory: "1024Mi"
            cpu: "500m"
        envFrom:
          - secretRef:
              name: env-secret
            configMapRef:
              name: env-config
            secretRef:
              name: aws-secret
        ports:
          - containerPort: 8080

