apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-user
  labels:
    run: api-user

spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-user

  template:
    metadata:
      labels:
        app: api-user

    spec:
      containers:
      - name: api-user
        image: beakalyk/udagram-api-user:v1a
        imagePullPolicy: Always
        resources:
          requests:
            memory: "200Mi"
            cpu: "250m"
          limits:
            memory: "2024Mi"
            cpu: "500m"
        ports:
        - containerPort: 8080
      restartPolicy: Always